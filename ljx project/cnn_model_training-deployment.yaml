apiVersion: apps/v1
kind: Deployment
metadata:
  name: cnn-model-training-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cnn-model-training
  template:
    metadata:
      labels:
        app: cnn-model-training
    spec:
      containers:
      - name: cnn-model-training
        image: your-registry/cnn-model-training:latest
        ports:
        - containerPort: 80
